#include "./UserInput.hpp"
#include <array>

std::array<UserInput::bindings,4> UserInput::keyBindings(){

    std::array<UserInput::bindings,4> keys = [
        {   },
        {   },
        {   },
        {   }
    ]

    return keys;

}
